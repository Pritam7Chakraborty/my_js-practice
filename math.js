//math -> built in object that provides a collection of properties of mathematics

let x = 3;
let y = 2;
let z = 1;

/*  z = math.round(x);
    z = math.floor(x);
    z = math.ceil(x);
    z = math.trunc(x);
    z = math.pow(x,y);
    z = math.sqrt(x);
    z = math.log(x);
    z = math.sin(x);
    z = math.cos(x);
    z = math.tan(x);
    z = math.abs(x);
    z = math.sign(x);
*/  
let max = Math.max(x,y,z);
let min = Math.min(x,y,z);

console.log(max)